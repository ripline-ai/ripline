{
  "id": "pipeline-orchestrator",
  "name": "Pipeline Orchestrator",
  "description": "Graph-based workflow engine and tools for OpenClaw",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "pipelinesDir": {
        "type": "string",
        "description": "Directory that contains pipeline *.json or *.yaml files"
      },
      "maxConcurrency": {
        "type": "integer",
        "minimum": 1,
        "default": 4,
        "description": "Maximum concurrent pipeline runs managed by the plugin"
      },
      "httpPath": {
        "type": "string",
        "default": "/pipelines",
        "description": "Base path for the plugin's HTTP API (e.g. for dashboards or visualizations)"
      },
      "httpPort": {
        "type": "integer",
        "minimum": 1,
        "maximum": 65535,
        "default": 4001,
        "description": "Port for the HTTP API server when started by the plugin"
      },
      "authToken": {
        "type": "string",
        "description": "Optional bearer token for API authentication (if set, Authorization: Bearer <token> required)"
      },
      "runsDir": {
        "type": "string",
        "default": ".ripline/runs",
        "description": "Directory for run artifacts (run state, run.json per run). Relative paths resolve from the workspace. Default: .ripline/runs"
      },
      "agentRunner": {
        "type": "object",
        "description": "When set and not running inside OpenClaw, agent nodes use this LLM (Ollama, OpenAI, or Anthropic) instead of the stub. Ignored when OpenClaw runtime is present.",
        "properties": {
          "provider": {
            "type": "string",
            "enum": ["ollama", "openai", "anthropic"],
            "description": "LLM provider"
          },
          "model": {
            "type": "string",
            "description": "Model name (e.g. llama3.2, gpt-4o-mini, claude-3-5-sonnet-20241022)"
          },
          "apiKey": {
            "type": "string",
            "description": "API key for OpenAI or Anthropic (optional for Ollama). Can also be set via OPENAI_API_KEY or ANTHROPIC_API_KEY env."
          },
          "baseURL": {
            "type": "string",
            "description": "Optional base URL (e.g. custom Ollama or OpenAI-compatible endpoint)"
          }
        },
        "required": ["provider", "model"]
      }
    },
    "required": ["pipelinesDir"]
  },
  "uiHints": {
    "pipelinesDir": { "label": "Pipelines directory", "placeholder": "./pipelines" },
    "runsDir": { "label": "Runs directory", "placeholder": ".ripline/runs" },
    "maxConcurrency": { "label": "Max concurrent runs" },
    "httpPath": { "label": "HTTP base path" },
    "httpPort": { "label": "HTTP port" },
    "authToken": { "label": "API auth token (optional)" },
    "agentRunner": { "label": "Standalone agent (when not using OpenClaw)" }
  }
}
